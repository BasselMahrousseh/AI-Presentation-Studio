# AI-Presentation-Studio

## Working with this user

The user is an MIS (Management Information Systems) student with some coding ability but limited background in software engineering, APIs, and LLMs — and wants to actually learn these concepts over time, not just get them hidden. When explaining technical concepts, findings, or decisions, keep it plain and systematic — one or two sentences per point rather than long technical prose — but still introduce and briefly explain the relevant term or concept rather than skipping it entirely.

## Origin

This repo is a fork of the open-source presentation generator from **presenton.ai** — the Next.js `package.json` name is still `presenton`, and much of the generic branding/module naming traces back to that upstream project. The team is incrementally layering custom features and branding (an "e&"/Etisalat-branded smart-deck mode, UI reskins, etc.) on top of that inherited base rather than building from scratch. When something looks orphaned, duplicated, or inconsistently named, check whether it's inherited upstream functionality that predates the e&-specific work before assuming it's a local bug.

Working branch: `restructured-v3-branch`, created off `restructured-v3`. See pinned memory for the git workflow rule (never commit/push directly to `restructured-v3`).

## Architecture

Two servers: `servers/fastapi` (backend, :8000) and `servers/nextjs` (frontend, :3000), talked to by the browser through `proxy.ts` (a Next.js "Proxy," the v16 rename of middleware) which rewrites `/api/v1/*` and `/api/v2/*` to FastAPI.

**Two parallel slide-content models — know which one you're in before changing generation code:**
- **TemplateV2 (structured)**: slides are JSON — a `content` object plus a `ui` element tree (`text`, `image`, `chart`, `table`, `text-list`, `flex`, `grid`, ...), schema-constrained per layout. Bundled templates live at repo-root `/templates/*/template.json`, seeded into the `template_v2` SQL table on startup. Rendered via `lib/template-v2-json-to-html.ts` (flat HTML) or a Konva canvas editor (`components/slide-editor/*`) for drag/resize editing. This is the **"standard"** generation path (`stream_presentation`, `?type=standard`) — each slide is its own sequential LLM call, so it genuinely streams slide-by-slide.
- **Smart HTML (raw)**: the LLM outputs a Tailwind `<section>` fragment per slide directly, rendered via `SmartHtmlSlide.tsx`'s iframe + browser-side Tailwind runtime (`lib/tailwind-browser.ts`). No schema, no structured editor — editing happens via the in-editor AI chat rewriting raw HTML (feature exists but its toggle button is commented out in `PresentationHeader.tsx`, so it's not discoverable). This is the **"smart"** path (`_stream_smart_presentation`, `?type=smart`) — for reasoning models the whole deck is typically one LLM call that "thinks" silently for a long stretch, then emits all slides in a fast burst. The e& brand template (`smart_brand_templates.py`) is *not* a TemplateV2 template — it's hardcoded literal HTML strings spliced around LLM-generated content.

**The live `/generation` page's button labels don't match this `standard`/`smart` backend terminology — don't infer generation mode from a UI button's label without checking `GenerationPageClient.tsx`.** `/generate` redirects straight to `/generation` (`app/generation/GenerationPageClient.tsx`), which has three actions, none of which is a "pick TemplateV2 vs Smart" toggle: **"Outline"** is the only button that actually sends `generation_mode: "standard"` (real TemplateV2), routing through `/outline` first rather than streaming a deck directly; **"Generate Standard"** is misleadingly named — it sends `generation_mode: "smart"` with no `smart_template` (plain Smart HTML, no e& branding); **"Generate e& deck"** also sends `generation_mode: "smart"`, just with `smart_template: "eand"`. A separate, older mode-picker component (`GenerationModeDialog.tsx` under `(presentation-generator)/upload/`) does correctly present "Standard" vs "Smart" as a real toggle. **Correction to an earlier claim in this file: that page is not dead** — `/generate`'s redirect bypasses it, but `/upload` is independently reachable from four other live entry points (onboarding's "Go to Upload" button, the outline page's empty state, the community page's "New presentation"/"Use this design" actions, and the presentation editor's error-recovery fallback). See the route reachability map below before assuming either `/generation` or `/upload` is "the" generation entry point — this app has two, with different mode-selection UIs.

**e& brand template specifics**: `smart_brand_templates.py`'s `EAND_FIXED_SLIDE_COUNT` (2 — the cover + thank-you slides, spliced in as literal HTML outside the LLM's own output) is *added on top of* the requested slide count, not subtracted from it — `n_slides`/a user's stated count always means content slides only for e& generation. (This was previously backwards — a request for "5 slides" silently produced only 3 content slides — fixed by having `determine_smart_slide_count()`/`resolve_smart_slide_count()` return the content count directly and having the caller in `presentation.py` add `EAND_FIXED_SLIDE_COUNT` for the reported total, instead of subtracting it out.) Content color palette is normally the hardcoded default in `get_smart_brand_prompt()`, but if the user attaches a `.pptx` among their source files when generating an e& deck, `templates/pptx_color_extraction.py` automatically extracts that file's real colors (ranked by actual shape-fill usage frequency across the deck — most real decks, including every deck this app itself exports, never touch their PowerPoint theme colors, so theme-color extraction alone returns generic Office defaults) and uses them instead of the default palette. There's no dedicated upload UI for this; it's inferred automatically from an already-attached file's `.pptx` extension.

The *default* palette (red/dark-blue/white/black, no uploaded reference deck) is deliberately narrow for the rest of the UI, but that narrowness broke charts specifically: a 5+ category pie/bar chart ran out of distinct colors, repeating red and/or using white (which disappears against the white slide/card background). Fixed with a **chart-only** expanded palette block in `get_smart_brand_prompt()` (`CHART COLOR PALETTE` section, explicitly scoped to `<canvas>` configs only — every other element still uses only the 4-color base palette): the base red/dark-blue plus computed 35%-white-tinted shades of each (`_tint_hex()`), plus Etisalat's own pre-"e&" heritage brand green `#82AA40` (with a lighter `#CCDB3B` accent) — independently sourced from the logo-color-extraction site coloropedia.com ("Sushi"/"Wattle"), corroborated by a separate "Lime Green Pantone 381C #CADB2A" reference, since this isn't part of the post-rebrand e& UI palette and needed verifying rather than guessing. This expansion only applies to the *default* palette — an uploaded reference deck's extracted `custom_colors` are left untouched, since those already represent that deck's real brand and shouldn't have invented colors layered on top. Note this is a **prompt-level** fix (steering what colors the LLM picks for `backgroundColor`/`borderColor` when it authors the Chart.js config), not something the post-export pipeline can retroactively correct — by the time `pptx_native_chart_service.py` sees a chart, whatever colors the LLM chose are already baked into both the live in-app preview and the eventual export, so fixing it at the source is what fixes both consistently.

**Smart-mode overflow validation**: `smart_slide_layout.py`'s `inspect_smart_slide_layout()` runs four deterministic, no-rendering heuristics (regex/Tailwind-class analysis via Python's stdlib `html.parser`, not a real layout engine) on every Smart slide before it's accepted, rejecting ones that look likely to overflow and forcing an LLM retry: `_find_shrink_to_fit_overflow_risks` (fixed-height flex-col + `flex-1 min-h-0` + numbered `grid-cols-N≥3`), `_find_shrink_marked_grid_overflow_risks` (broader — CSS's default `flex-shrink:1` means `min-h-0` alone is enough, without needing `flex-1`; also covers arbitrary `grid-cols-[...]` templates and a fixed height implied by `position:absolute` with both `top-*`/`bottom-*` set, not just an explicit height class), `_find_fixed_height_card_text_overflow_risks` (a card's own text length vs. its declared height, width-scaled by the row's column count), and `_find_stacked_list_overflow_risks` (4+ stacked text items in an `h-full`/fixed-height panel). None of these can measure real normal-flow layout — that's structurally impossible without an actual browser — so each is a calibrated proxy, not a guarantee; every one was added after a specific real production slide overflow slipped past the others (verify any new one against real HTML pulled from `app_data/fastapi.db`'s `slides.html_content` before trusting it). Separately, `generate_smart_presentation.py`'s `_check_smart_slide_eand_footer_safe_area` (e&-template-only) actually renders each slide through the Puppeteer-backed export runtime and checks pixels, since the reserved `y≥630` footer zone can't be caught by class analysis at all — the one overflow check in the system that costs real generation latency (a few seconds per e& slide) for a real measurement instead of a heuristic.

**SSE streaming**: `proxy.ts`'s `NextResponse.rewrite()` silently buffers SSE responses (confirmed via direct backend testing — real per-slide gaps at the source, one burst through the browser). The three stream paths (`/api/v1/ppt/presentation/stream/`, `/api/v2/ppt/presentation/stream/`, `/api/v1/ppt/outlines/stream/`) are excluded from that rewrite and instead hit dedicated Route Handlers (`app/api/v1/.../stream/[id]/route.ts`, via `lib/sse-proxy.ts`) that pipe the FastAPI response through untouched. If a new SSE endpoint is ever added, it needs the same treatment or it will silently stop streaming in real time.

**Auth**: session cookie + `sk-presenton-*` API-key bearer tokens, one role bit (`is_superuser`). When `DISABLE_AUTH` is set, the middleware bypasses auth entirely and `/api/v1/auth/status` hardcodes `username: "electron", role: "admin"` — this is a generic single-user bypass mode, not literally tied to the Electron app (which no longer exists in this fork, see below).

**Async jobs**: `AsyncTaskModel` is genuinely used for template creation (polled by the frontend). The equivalent async wrapper for presentation generation (`/generate/async`, `/status/{id}`) is confirmed dead code — nothing in the frontend calls it; the SSE streaming endpoints are what's actually used.

**Single-slide editing**: `POST /api/v1/ppt/slide/edit` (TemplateV2) and `/slide/edit-html` (Smart HTML) are prompt-driven, single-slide endpoints — now used directly by the slide action bar's "Regenerate Slide" button (optional feedback text gets appended to a fixed default prompt), and internally by the AI chat's tool-calling edits. `edit_slide` used to call `presentation.get_layout()` directly, which throws for any presentation whose `layout` column isn't the legacy single-layout shape — most presentations today store either `None` or a `{layouts: [...]}` template-picker payload instead, so this 500 went unnoticed since the endpoint had zero frontend callers before. Fixed via a shared `utils/presentation_layout_resolver.py` (also now used by `services/chat/memory_layer.py`, which already handled both shapes correctly) — any new code needing a presentation's resolved layout should call `resolve_presentation_layout_model()`, never `presentation.get_layout()` directly.

**Generation loading UI**: the header progress pill (`PresentationHeader.tsx`) and the pre-first-slide `OverlayLoader` modal (shown from `PresentationPage.tsx`) both derive their status text from one shared helper, `getStreamProgressLabel()` (`app/(presentation-generator)/presentation/utils/streamProgressLabel.ts`), fed by real SSE-reported `streamTotalSlides`/`streamStageMessage` Redux state — so they can't drift out of sync with each other again. `ProgressBar`/`OverlayLoader` also gained an opt-in `indeterminate`/`indeterminateProgress` mode (an honest "still working" sweep, no fabricated percentage), used only by this modal — the same shared components still default to the old fake elapsed-time percentage for 3 other loading screens (upload, outline, document preview) that weren't touched.

**Export**: neither server renders PPTX/PDF itself — both shell out to the same `presentation-export/` prebuilt Node bundle (Puppeteer-based) as a subprocess, pointed at the live `/pdf-maker` page. That bundle needs the repo-root `node_modules` installed (specifically `sharp`) — **`npm install` at the repo root** (not just `servers/nextjs`) is required and is not mentioned in README's setup steps. If PPTX/PDF export fails with `Cannot find module 'sharp'`, this is why.

**There are two separate, independent callers of that bundle — know which one a given export actually went through before assuming either side's export code ran.** `services/export_task_service.py` (FastAPI) is used by API-style endpoints that generate-and-export in one request (`presentation.py`'s `/edit`, `/derive`, and the main generate flow, all calling `utils/export_utils.py::export_presentation()`). But the interactive **"Export PPTX"/"Export PDF" button in the presentation editor** (`PresentationHeader.tsx`) calls a completely different, parallel implementation: the Next.js route `app/api/export-presentation/route.ts` → `lib/run-bundled-presentation-export.ts`, which spawns the bundle itself from the Next.js process and never touches FastAPI at all. Both build an equivalent `{type:"export", url, format}` task JSON and point at the same `/pdf-maker` URL, but they are two separate, duplicated code paths — a fix or feature added to one (e.g. a query param threaded through the export URL) does nothing for the other unless added to both. This was discovered the hard way: the native-chart-export feature below was originally wired only into `export_utils.py` and silently never ran for real interactive exports, since those never go through it. If FastAPI logs show zero export activity for an export you just triggered from the editor UI, this is why — check the Next.js terminal/`[bundled-export]`-prefixed logs instead.

That downloaded bundle (`presentation-export/`, gitignored — **never edit it directly**, changes vanish on the next `npm install`/re-sync) is also **patched**, not used verbatim: `scripts/sync-presentation-export.cjs`'s `KNOWN_BUNDLE_PATCHES` applies small string-replacement fixes for known upstream bugs after every download (currently one: a fully-rounded non-square element, like a pill badge, was misclassified as a circle and exported as a squashed oval in PPTX — patched in the JS shape-classification logic). Patches are pinned to the exact `presentationExportVersion` in `package.json` and skip themselves with a console warning (not a hard failure) if the bundle's code no longer matches, so bumping that version needs each patch re-verified against the new bundle before it'll take effect again. Two other real PPTX export bugs were found and are **not** fixable from this repo: a text run's own inline color/font-size (e.g. a bold red number sitting inline next to smaller plain text) gets flattened to the surrounding text's style on export, and some decorative elements land off-center. Both trace into `presentation-export/py/convert-*`, a compiled PyInstaller Python binary with no available source — fixing them needs either the upstream `presenton/presenton-export` source or reverse-engineering the binary, both out of reach here. The practical workaround is avoiding the triggering pattern in generated HTML (e.g. giving an emphasized number its own block instead of an inline span mixed with differently-styled text) rather than trying to fix the converter.

**Electron was removed** from this fork in commit `5d4665f` ("Removed electron," ~Aug 22). Vestiges remain and are currently broken/stale, not yet cleaned up:
- `Dockerfile` and `Dockerfile.dev` both still `COPY electron/resources/...` — a from-scratch Docker build currently fails at that step.
- `.github/workflows/electron-linux-ubuntu22.yml` still tries to build the removed `electron/` directory.
- Frontend Electron-detection code (`isElectronRuntime()`, `window.electron?.exportPresentation`) still exists throughout the Next.js app but is now vestigial — nothing ever sets `window.electron`.

## Route reachability map (Next.js)

This app carries a lot of well-built, upstream presenton.ai code that the live e& product no longer routes to. Twice in one session an assistant gave wrong answers about the live UI by finding and trusting genuinely good code that turned out to be unreachable. **Before trusting any `page.tsx` you find by name/keyword search, grep the entire `app/` + `components/` tree for that route's literal path string (`"/routename"`, `` `/routename` ``) used in `Link href=`, `router.push/replace`, or `redirect`.** If the only hits are inside the route's own subtree, it's very likely orphaned.

**Live, reachable routes:**
- `/` (`app/page.tsx`) — the real app home (`DashboardShell` → `DashboardPage`). Everything else is reached from here or from deeper pages.
- `/generate` → redirects to `/generation`. `/dashboard` → redirects to `/`. Both are legacy-URL shims kept for old links/buttons, not meaningfully different pages.
- `/generation` (`GenerationPageClient.tsx`) — the **primary** generation entry, linked from the dashboard's "Create with AI" button. Three-button UI: Outline / Generate Standard / Generate e& deck (see the button-label note above — none of those labels match the backend `standard`/`smart` split cleanly).
- `/upload` (`UploadPage.tsx`) — a **second, independent** generation entry with a real Standard/Smart `GenerationModeDialog.tsx` toggle. Not linked from the dashboard's main CTA, but reachable from: first-time onboarding's "Go to Upload" button, the outline page's empty state, the community page's "New presentation"/"Use this design" actions, and the presentation editor's error-recovery fallback. Two files inside its `components/` folder are true dead leftovers with zero importers: `LanguageSelector.tsx`, `NumberOfSlide.tsx`, plus an unused `styles/main.module.css`.
- `/outline`, `/presentation` (needs `?id=`), `/custom-template`, `/template-preview`, `/community`, `/theme`, `/settings` — all confirmed live, standard parts of the generation/editing pipeline and dashboard.
- `/templates` — reachable, but the two persistent "Templates" sidebar/nav links to it are intentionally disabled no-op buttons (`onClick={() => undefined}`); the page is still landed on programmatically after custom-template creation finishes.
- `/admin` (`AdminPanel`) — has zero in-app `Link`/`router.push` anywhere, reachable only by typing the URL directly. Not dead code, though: the same `AdminPanel` component is actively used, embedded as a tab inside `/settings`. If asked "where's the admin panel," point at the `/settings` tab, not this standalone route.
- `(export)/pdf-maker` — live, but only as a **headless render target** for the export pipeline (`lib/run-bundled-presentation-export.ts` launches Puppeteer against it). No user-facing link points here; don't treat the absence of one as meaning it's dead.

**Confirmed dead/orphaned — inherited from upstream, do not treat as representative of the live app:**
- `app/frontend/` (`page.tsx` + `index.tsx`) — a full standalone alternate dashboard/generation prototype (renders the upstream Presenton onboarding wizard via `components/Home.tsx`, or an entirely separate 700-line prototype in `index.tsx` that isn't even imported by its own `page.tsx`). Zero inbound links anywhere in the app; its "Generate" action also hardcodes `http://127.0.0.1:8000`, bypassing the Next.js proxy entirely, which wouldn't work in any real deployment anyway.
- `/documents-preview` (`DocumentPreviewPage.tsx`) — fully wired (calls `router.replace("/outline")` internally) but nothing links into it anymore; only referenced defensively in `Header.tsx`'s back-button path-matching array.
- `components/DashboardNav.tsx` — an alternate sidebar variant, never imported anywhere.
- Dead Next.js API routes (zero callers found in either the Next.js frontend or the FastAPI backend): `api/github-stars`, `api/has-required-key`, `api/templates` (the plain, non-`v1` one — the directory it reads, `app/presentation-templates/`, doesn't even exist on disk), `api/upload-image` (real image uploads go through `api/v1/ppt/images/upload` on FastAPI instead).

## Session backlog (prune as items are done — don't let this go stale)

From an end-of-session review, priority-ordered per UX/UI-first preference:

1. **Surface the AI click-to-edit feature.** It's fully wired for smart-mode decks but its header toggle button is commented out — the most useful recovery path for a broken slide is currently undiscoverable.
2. **Improve export failure messaging.** Currently a generic "Export failed" toast; worth checking whether failure modes beyond the `sharp` crash are similarly opaque.
3. ~~Charts/graphs in exported PPTX~~ **Done — implemented and confirmed working end-to-end.** A post-export pass (`services/pptx_native_chart_service.py`) swaps flattened chart images for native, editable PPTX chart objects, driven by chart data captured from the live, resolved Chart.js instance on the export page (`lib/chart-export-capture.ts`) — covers both TemplateV2 and Smart-mode charts uniformly, since by render time both are just a `<canvas>` with a real Chart.js instance attached. `polar_area`/`scatter`/`bubble` intentionally stay as flattened images (no faithful native PPTX equivalent).

   Two real bugs surfaced during live testing and are both fixed:
   - **The capture token never reached the export page at all.** The feature was wired only into `export_utils.py`'s `export_presentation()`, but the interactive "Export PPTX" button never calls that — it goes through the entirely separate `lib/run-bundled-presentation-export.ts` (see the Export architecture note above). Fixed by adding the same token-minting to that Next.js path and a new `POST /api/v1/ppt/presentation/export/upgrade-charts` endpoint it calls after producing the file (validated to only ever touch paths inside `APP_DATA_DIRECTORY/exports`).
   - **The capture report request itself hung every PPTX export** until the export bundle's full 120s Puppeteer navigation timeout, reproducibly. Root cause, confirmed by direct reproduction + bisection (restarting both servers under direct control, curling the export endpoint myself, and disabling pieces of the capture code one at a time until the hang disappeared): the export bundle navigates with `page.goto(url, {waitUntil:"networkidle0"})` (confirmed by grepping the vendored bundle), and **any** same-origin `fetch()`/XHR fired from the page while that wait is still pending can make Puppeteer's network-idle tracking never settle — even a request that itself completes quickly and successfully, and even with `keepalive` removed (a first fix attempt that looked plausible but didn't work; don't re-try it). Fixed by switching the report call from `fetch()` to `navigator.sendBeacon()`, which is purpose-built for "notify the server without disrupting page lifecycle" and sidesteps this. **Lesson for any future code that runs inside the export page**: never fire a same-origin `fetch()`/XHR from within `/pdf-maker` while the page is still loading — use `sendBeacon` instead, or defer until well after load.

   Verified against a real 10-slide deck by restarting both dev servers under direct control and calling `/api/export-presentation` directly, then re-opening the produced `.pptx` with `python-pptx`: all 5 supported chart types (area, horizontal_bar→BAR_CLUSTERED, donut→DOUGHNUT, bar→COLUMN_CLUSTERED, radar→RADAR_MARKERS) matched their picture shapes with IoU=1.000 and carried byte-for-byte correct category/series data; the 1 scatter chart correctly remained a picture.

   **Follow-up visual-polish pass** (native PPTX charts are editable but visually plainer than the original Chart.js rendering — PowerPoint's chart engine has a much smaller styling vocabulary, e.g. no rounded bar corners or gradients; that gap is permanent, but four things were fixable and are now done): per-category/per-point bar and pie/donut colors (Chart.js semantics: an array `backgroundColor` on a dataset means one color per point, previously only handled for pie/donut — `_POINT_COLOR_KINDS` in `pptx_native_chart_service.py`); native data labels (`plot.has_data_labels`, driven by a newly-captured `hasDataLabels` flag, default on per this app's own Chart.js generation contract); font matching (`chart.font.name`, from a newly-captured `fontFamily`); and category-axis order for horizontal bar charts (`chart.category_axis.reverse_order` — PowerPoint lists the first category at the bottom by default, Chart.js at the top). Axis/data-label unit-suffix formatting (e.g. Chart.js's `value => value + '%'` callback) was explicitly left out of scope — it's an arbitrary JS function this app can't generically introspect, so native labels show plain numbers instead.

   One real bug surfaced and is fixed: **font capture initially always returned Chart.js's own built-in default (`'Helvetica Neue', 'Helvetica', 'Arial', sans-serif`) instead of the deck's real font**, because Chart.js merges its complete default structure into every nested plugin's *resolved* runtime options — `chart.options.plugins.legend.labels.font.family` always resolves to *something* even though this app's charts never customize legend styling (legend is almost always `display:false`). `extractFontFamily()` in `lib/chart-export-capture.ts` was checking legend/title first, so it always won over the genuinely-customized `datalabels`/axis-ticks font. Fixed by reordering the check priority (datalabels and axis ticks first) and filtering out the known Chart.js default string outright. **General lesson**: a live Chart.js instance's `.options` is not a reliable signal for "did the author customize this" — it's always a complete, defaults-merged object; only trust it for values you know this app's generation prompts actually set, and validate any such assumption against the real captured payload (not just the final PPTX), which is exactly how this bug was actually found — `chart_capture.py` now has a permanent `LOGGER.debug("chart_capture: full payload=%s", ...)` line for exactly this kind of diagnosis in future sessions (invisible by default, set `LOG_LEVEL=DEBUG` to see it).

   Verified again end-to-end the same way (direct curl + real `python-pptx` re-read) after the polish pass: all 5 supported charts showed the deck's real font, enabled data labels, correct per-point/per-series colors, and `reverse_order` correctly set only on the horizontal bar chart.

   **Second polish pass: text color.** The font/color polish above still left every chart's axis labels, data-value labels, legend, and title in PowerPoint's default text color — invisible-ish against this app's typically dark chart backgrounds, since the original Chart.js rendering explicitly colors that text light (e.g. `ticks: {color:'#AFC3CE'}`, `datalabels: {color:'#FFFFFF'}`). Added `tickColor`/`dataLabelColor`/`legendColor`/`titleColor` capture (same "filter out Chart.js's own merged-in default" pattern as `fontFamily`, this time against `Chart.defaults.color` = `#666`) and applied them to `category_axis.tick_labels.font.color` / `value_axis.tick_labels.font.color` / `plot.data_labels.font.color` / `chart.legend.font.color` / `chart.chart_title...font.color` in `pptx_native_chart_service.py`. `pie`/`donut` are the only supported kinds with no category/value axis at all (`chart.category_axis` raises `ValueError` for them, confirmed empirically) — gated via `_NO_AXIS_KINDS`.

   **A scary-looking false alarm during verification, worth remembering**: the very first live export after restarting both dev servers with this change came back with every color field silently missing — same "no color found, leave default styling" safe-skip path, just triggered by nothing. The *second* export, seconds later, with zero code changes in between, came back completely correct, and a third confirmed it was consistently correct from then on. Root cause: Next.js/Turbopack dev mode persists a compiled-output cache across process restarts, and the very first request to a changed route after a fresh `npm run dev` can serve a stale bundle while it recompiles in the background. **Lesson: after restarting the dev servers for verification, always throw away the first export and trust the second** — a single broken result immediately after a restart is not by itself evidence of a real bug.

   **Third polish pass, from real side-by-side screenshots of native output vs. the original**: (1) native charts had an opaque white chart-area/plot-area background instead of the original's transparent one, so a chart sitting on a dark card rendered as a jarring white box — python-pptx exposes no high-level API for this, fixed via direct OOXML manipulation (`_make_chart_background_transparent()`, inserting `<c:spPr><a:noFill/>...` at the two schema-mandated positions in `c:chartSpace`/`c:plotArea`, verified against python-pptx's own oxml schema classes and round-tripped through save/reload). (2) pie/donut slices showed only a bare percentage with no indication of which category it was (the side legend was the only cross-reference) — fixed via `data_labels.show_category_name = True` alongside `show_percentage = True` (PowerPoint's own computed share-of-whole, semantically correct for any pie/donut regardless of whether the source values already happen to be percentages), scoped to pie/donut only. (3) axis titles (e.g. "% of companies") had no captured/applied color, same class of bug as tick/data-label color before it — added `axisTitleColor` capture and application, same pattern.

   **A second, different-flavored false alarm during this round's verification — do not confuse with the Next.js caching one above.** After editing only backend Python (`pptx_native_chart_service.py`), a "restart" of FastAPI silently failed to bind (`[Errno 48] address already in use`) and exited, while an *old* FastAPI process from an earlier verification round was still running unnoticed on port 8000 — so every subsequent export kept hitting genuinely stale backend code (explaining exactly the observed pattern: color/font fixes from the *previous* round worked, but this round's brand-new transparency/pie-label code silently didn't, because that old process predated it). Next.js had the same problem independently (`⚠ Port 3000 is in use... Another next dev server is already running`). **Lesson: `nohup cmd &` "succeeding" only means the shell backgrounded it — it does NOT mean the process actually bound the port.** Before trusting any restart for verification, confirm the *new* PID is actually listening (`lsof -i :PORT -sTCP:LISTEN`) and that its own startup log shows a clean bind (FastAPI: "Uvicorn running on..."; Next.js: "Ready in..."), not an "address already in use" error further down the same log. When in doubt, `lsof -ti:PORT | xargs kill -9` on *every* relevant port (3000, 8000, and any Next.js fallback port like 3001) before restarting, not just the one that looked wrong.
   **"All charts still export as flattened images" — root-caused and fixed (two independent bugs, both in `lib/chart-export-capture.ts`).** Reproduced directly: a real 12-slide deck with 3 supported charts (1 bar, 2 line) exported with **0 of 3** native. The transport chain was proven healthy first (a hand-rolled `curl` to `/api/export-chart-capture` wrote a capture file correctly), which isolated the fault to the browser-side capture on `/pdf-maker`. Driving that page under Playwright with the console attached surfaced both faults:
   - **Bug 1 — one chart with a scriptable option killed the whole deck's capture.** Console showed `Skipped native chart capture TypeError: Cannot read properties of undefined (reading 'borderColor')`. Chart.js's `chart.options` is not a plain object but a **lazily-resolving proxy: merely *reading* a property executes a scriptable option the slide's author wrote**. One chart used `datalabels: { color: (c) => c.dataset.borderColor }` (real LLM output), and reading `options.plugins.datalabels.color` outside a real draw call threw (`Recursion detected: color->color`). `extractDataLabelColor` had a comment claiming scriptables were handled — but its guard was `isRealColor`'s *typeof* check on the **value**, which never runs because the **property access itself** throws first. Worse, `captureAllChartsOnPage` wrapped the entire per-canvas loop in a *single* try/catch, so that one throw discarded every good chart already collected and sent nothing at all. Fixed with (a) a `safeRead()` guard around every resolved-options read — taking a *getter*, not an already-read value, so the whole property chain (`scales.x.ticks.color`, not just the leaf) is inside the try — and (b) a per-chart try/catch in the capture loop, so one bad chart now costs only that chart. **General lesson: on Chart.js resolved options, a defensive check on the value is not a guard — the read is the dangerous part.** This took the deck from 0/3 to 1/3.
   - **Bug 2 — captured geometry was measured before the page finished settling.** The remaining 2 charts *were* captured correctly but the backend refused them: their exported picture matched the captured box exactly in left/width/height while being offset vertically by 17.87px and 24.0px, scoring IoU 0.894 and 0.858 against the deliberate `_IOU_MIN=0.90` "fail closed rather than guess" threshold. Timeline probing found the cause: **`document.fonts.status` reads `"loaded"` at the moment charts first report rendered, then a *second* wave of font loading starts ~300ms later and completes ~900ms in**, reflowing the text above each chart and pushing it down by exactly those amounts — while the export bundle screenshots the *settled* position much later. So a single `await document.fonts.ready` resolves during that lull and is useless here (confirmed empirically — an initial fix that did exactly that changed nothing). Fixed with `waitForStableChartLayout()`: poll every 100ms and require **8 consecutive stable rounds (~800ms of continuous stillness)**, treating `document.fonts.status === "loading"` as automatically not-settled, bounded by a 5s timeout that fails open. Deliberately timers + layout reads only — no fetch/XHR, per the `networkidle0` hazard above. **Do not "fix" this class of mismatch by loosening `_IOU_MIN`**; the geometry was genuinely wrong and a looser threshold would risk binding a chart to the wrong picture. This took the deck from 1/3 to **3/3**.

   Verified end-to-end by real export (not mocks) twice in a row after the fix: all 3 charts became native `LINE_MARKERS`/`BAR_CLUSTERED`/`LINE_MARKERS` with byte-correct categories and values. Test coverage added in `tests/chart-export-capture.test.mjs` (13 passing) using a `throwingGetter()` helper that mimics a property throwing on read, covering both "extractors survive a throwing scriptable" and "a chart with a throwing option is still captured, minus just that one style."

   **PPTX-corruption-with-lost-slide bug — finally root-caused and fixed.** An earlier session's investigation (below, preserved for the record) could not reproduce this without the user's actual pre-repair file. This session got that file (a real "Chart Stress Test" 14-slide deck covering all 9 native-supported chart kinds, still on disk at `app_data/exports/Chart-Stress-Test_443f16fb-afbe-417f-8208-e570596dec02.pptx`) and found it immediately: the doughnut chart's part (`ppt/charts/chart8.xml`) contained `<c:dLblPos val="outEnd"/>` — **PowerPoint rejects a data-label position on a doughnut chart outright**, the file fails to open ("needs repair"), and PowerPoint's own recovery drops the entire slide. The sibling pie chart in the same file (`chart7.xml`) carried the *identical* `outEnd` value and opened fine — `outEnd` is legal for `<c:pieChart>`, not for `<c:doughnutChart>` — and a structural diff of the two chart parts (element-by-element, ignoring the expected `c:doughnutChart`/`c:holeSize`/`c:firstSliceAng` differences) found no other meaningful difference between them. `_apply_chart_styling()` in `pptx_native_chart_service.py` had one shared branch for `kind in ("pie", "donut")` that set `data_labels.position = XL_LABEL_POSITION.OUTSIDE_END` for both — python-pptx's own `DataLabels.position` setter (`pptx/chart/datalabel.py`) does zero chart-type validation, so nothing caught this before the file reached PowerPoint. Fixed by splitting the branch: only `kind == "pie"` gets `OUTSIDE_END` (gated by a new `_OUTSIDE_END_LABEL_KINDS = {"pie"}` constant, deliberately a set of one rather than folded into an existing broader constant, so any future addition is a conscious decision); donut gets `show_percentage=True, show_value=False, show_category_name=False` with **no position set at all**, letting PowerPoint choose its own legal default (inside the ring). The earlier investigation's exhaustive schema-order checks were real and correct — they just never got to test the one specific combination that was actually broken, because they never had the real file to work from. **Lesson reinforced: for a corruption bug, get the actual pre-repair file before spending more time on speculative reproduction** — every other angle this session tried (structural diff, schema-order re-verification) merely confirmed what the file itself already showed in under a minute of inspection.

   Same investigation surfaced two more, unrelated real bugs in the same function, both confirmed in the same broken file's XML:
   - **Data labels were invisible on every non-circular chart.** `plot.has_data_labels = True` alone emits a `<c:dLbls>` whose `<c:showVal>` defaults to `"0"` — nothing ever flipped it for bar/line/area/radar (only the pie/donut branch touched any `show_*` flag). Confirmed: the area chart's `chart6.xml` had `<c:showVal val="0"/>`, i.e. a data-label block that displayed literally nothing. **A previous session's own verification of this exact code checked the python-pptx *property*, not the *emitted flag*, and missed it entirely** — another instance of the same lesson as the `python-pptx re-reading successfully ≠ correct` rule already documented below. Fixed with an `else: data_labels.show_value = True` for every non-pie/donut kind.
   - **No font size was ever set on any native chart element** — only `chart.font.name` (family) and various `.font.color` were written anywhere in the file; grepping for `font.size`/`Pt(` returned zero hits. Every text element fell back to python-pptx's own chart-creation template, which (confirmed by building a chart via `add_chart()` with zero styling calls and counting) always bakes in exactly one `sz="1800"` (18pt) at the chart-wide `c:chartSpace/c:txPr` level — nowhere near the deck's actual `10-14px` (≈7.5-10.5pt) Chart.js sizes, which is why native pie labels rendered dramatically oversized and one ("Direct 5%") visibly overlapped the pie. Fixed end-to-end: `chart-export-capture.ts` now also captures `dataLabelFontSize`/`tickFontSize`/`titleFontSize`/`axisTitleFontSize` (mirroring the four existing color fields' getter-chain/priority-order pattern exactly, through the same mandatory `safeRead()` guard), and `pptx_native_chart_service.py` converts CSS px → points via a new `_pt_from_css_px()` (`px * 0.75`, exact and lossless since 1280px canvas = 13.333in = 960pt) and applies them to the chart-wide default, legend, title, data labels, axis tick labels, and axis titles. **Deliberately no "filter out Chart.js's own default" step for size**, unlike the existing font-family/color filters: Chart.js's default size (12) is genuinely what it rendered at, not a placeholder for "never customized" — filtering it out would silently reintroduce the 18pt bug for exactly the charts most likely to need the fix.

   **Two more visual defects fixed in the same pass, both confirmed against the real broken file's XML:** (1) the legend was hardcoded to `XL_LEGEND_POSITION.BOTTOM` regardless of the source Chart.js config — the stress-test pie/donut both used `position: 'right'`, and forcing it to the bottom squeezed the plot area, worsening the label-crowding in the font-size bug above. Now a captured `legendPosition` (one of `top`/`left`/`bottom`/`right`) is mapped via a new `_XL_LEGEND_POSITION_BY_NAME` dict, defaulting to bottom only when absent/unrecognized. (2) the area chart's translucent fill (`backgroundColor: 'rgba(53,208,186,0.28)'` in the source) rendered fully opaque — confirmed zero `<a:alpha>` anywhere in the broken file. `_hex_from_css_color()`'s `rgba(...)` branch already discarded the 4th component by design (`parts[:3]`), and `fill.solid()` + `fore_color.rgb` is always fully opaque; python-pptx has no high-level alpha API. Fixed with a new `_alpha_from_css_color()` (kept deliberately separate from `_hex_from_css_color` — several call sites and its own pinned tests depend on that function's exact existing signature) plus `_apply_fill_alpha()`, which appends `<a:alpha val="{alpha*100000}"/>` directly to the `<a:srgbClr>` element via the same raw-lxml idiom already used by `_make_chart_background_transparent()` — verified empirically that `ColorFormat.rgb`'s setter replaces the color element outright, so alpha **must** be applied after the color assignment, never before, or it's silently discarded. The per-series color path also needed a new `_series_raw_color()` helper alongside the existing `_series_color()`, since the latter's return value had already thrown away which raw string produced it (and thus its alpha) by the time the fill site could use it.

   **Two user decisions made explicitly for this pass, not inferred:** area charts stay native rather than falling back to a flattened image, accepting that PowerPoint's native area chart type genuinely cannot render point markers (a permanent, un-fixable gap, unlike everything else here); and donut labels show percentage only (no category name), since the corruption fix means donut labels can now only legally sit *inside* the ring, where a category name would crowd small slices — the category is still available from the legend beside the chart.

   Verified end-to-end against the real 14-slide stress-test deck, twice in a row (deterministic, byte-identical structural markers both times): all 9 supported chart kinds converted to native with **zero** `c:dLblPos` on the doughnut (both runs), correct `showVal`/`showPercent`/`showCatName` flags on every kind, `<a:alpha val="28000">` on the area chart's fill (matching the source's `0.28` alpha exactly), and real point-sized `sz` values (975/1050/900 = 9.75pt/10.5pt/9pt) replacing the universal 1800. Manually re-verified the doughnut's full `<c:dLbls>` element sequence against ECMA-376's `CT_DLbls`, both to confirm `dLblPos` is genuinely absent (not merely empty-valued) and that everything else is still in schema order. 64 new backend tests added to `tests/unit/test_pptx_native_chart_service.py` (65 → 129) (asserting on **emitted XML**, not just python-pptx properties, for every one of these — the whole bug class here is python-pptx silently accepting XML that PowerPoint rejects) and 4 new frontend tests to `tests/chart-export-capture.test.mjs` (13 → 17). **Now independently confirmed by the user**: opened the fixed export in real PowerPoint with no repair prompt, all 14 slides present — this is the one check python-pptx's own leniency about element ordering/validity can never provide by itself (that leniency is exactly the gap that let the original corruption ship undetected).

   **Follow-up, same session: area-chart data labels found sitting at the wrong height.** Confirmed by the user opening the fixed file: on the area chart specifically, each label appeared roughly at half its point's true height (e.g. "7200" rendering near where "3600" should be) rather than at the actual line/point. Investigated by dumping the real chart6.xml: **no `<c:dLblPos>` and no manual `<c:layout>` anywhere** — this is PowerPoint's own unconfigured default behavior for an area series' data labels (a well-documented Office quirk: area-chart labels default to the vertical midpoint of the filled region below the point, not the point itself), not something this codebase's code was setting incorrectly. Verified the mechanism by measuring the reported screenshot directly: the rendered height of each label matched `value / 2`, not `value`, closely enough (within normal rendering/estimation precision) to confirm the theory.

   This one required real caution given what the doughnut bug had just taught: Microsoft's own documentation states the `ABOVE`/`BELOW`/`LEFT`/`RIGHT` label positions are valid **only** for line/scatter/radar charts, and `INSIDE_END`/`OUTSIDE_END`-family positions only for bar/pie — guessing either family for area risked reintroducing the exact same "python-pptx accepts it, PowerPoint doesn't" corruption class just fixed. Presented this tradeoff to the user explicitly rather than guessing. **User's decision: try only the one value Microsoft documents as valid across every chart type with no exception — `CENTER` ("ctr")** — implemented via a new `_CENTER_LABEL_KINDS = {"area"}` constant (same deliberately-narrow, one-kind-at-a-time style as `_OUTSIDE_END_LABEL_KINDS`), gated the same way. Verified via two real re-exports (deterministic): the area chart's `<c:dLbls>` now emitted `<c:dLblPos val="ctr"/>`, with the deck's other 8 charts and slide count unaffected.

   **That "documented as universally safe" value corrupted the file too.** The user opened the new export in real PowerPoint and got the same "needs repair" prompt as the original doughnut bug — repair deleted the area chart's own slide. So `ctr` is **also** illegal for an area chart in real PowerPoint, directly contradicting the vendor documentation this decision was based on. Reverted immediately: `_CENTER_LABEL_KINDS` and its application site were removed entirely; an area chart's data labels now get **no explicit position at all**, back to the state that was already confirmed safe (the user had this exact deck open in PowerPoint with no repair prompt, evidenced by a `~$verifyfix2...pptx` PowerPoint lock file found alongside it, before the `ctr` attempt). Re-verified via two more real re-exports: zero `c:dLblPos` on the area chart, doughnut fix and all other fixes from this session untouched, all 9 charts still native. **The mis-positioned labels (the original cosmetic complaint) are back and unresolved** — accepted as a real, currently un-fixable limitation of this codebase's tooling, not abandoned by choice: every avenue tried or considered needs either a `dLblPos` value (now proven unsafe for area twice, by two different values, one of which came directly from Microsoft's own docs) or per-label manual-layout pixel offsets computed against the chart's real rendered plot-area geometry and axis scale, which nothing in this environment can determine without an actual PowerPoint rendering pass to measure against.

   **General lesson, stated as strongly as the file-corruption stakes deserve: for OOXML chart elements like `c:dLblPos`, treat *every* source as unverified until a real PowerPoint open confirms it — including official Microsoft documentation.** This session hit that gap twice on the same element: python-pptx performs zero chart-type validation (already known from the doughnut bug), and now Microsoft's own published compatibility table for `XlDataLabelPosition` also turned out to be wrong, or at least inapplicable to the OOXML file-format encoding of "ctr" specifically. Neither "the library accepted it" nor "the vendor's docs say it's fine" is evidence for this class of change. The only verification that has ever caught either bug is a human opening the actual file in real PowerPoint - budget for that step explicitly before shipping any future `dLblPos` (or similarly chart-type-restricted OOXML element) change, rather than treating documentation or library behavior as sufficient sign-off.

   (Original, pre-reproduction investigation from an earlier session, preserved for context on what was already ruled out: generated multiple real e& decks via the actual API/LLM pipeline and, once one succeeded, exported it and dumped the resulting chart XML for manual schema verification — checked `chartSpace`/`chart`/`plotArea` top-level ordering, a bar chart's axis-title placement inside `valAx`, and a pie/donut's `dLbls` internals against the real ECMA-376 `CT_DLbls` sequence. Every combination tested came back byte-for-byte correct, because the one broken combination — `dLblPos` specifically on a *doughnut* — was never among the synthetic decks generated to test with.)

   **Smart-mode heading-too-small bug — fixed.** A slide heading rendered noticeably smaller in the main editor canvas than in the sidebar thumbnail preview for the same slide, reproducible on "every slide basically." Root cause: two divergent Smart-mode renderers share one Tailwind JIT engine (`public/vendor/tailwindcss-browser-*.js`, a passive, `MutationObserver`-driven engine with no public "wait until rebuild is done" API). The sidebar's `SmartHtmlSlide.tsx` renders each thumbnail in its own isolated `<iframe>` with a fresh Tailwind compile every time, so it's always correct. The main canvas's `SmartHtmlEditor.tsx` instead injects raw HTML directly into the live app DOM (`container.innerHTML = html`) and relies on one shared, whole-page-scoped Tailwind engine — so any arbitrary-value class not yet seen elsewhere on the page (e.g. `text-[43px]`) briefly rendered unstyled/default-sized until that shared engine's async recompile caught up, with no code waiting for it. Fixed with the smaller, targeted fix (not the larger architectural change of switching the main canvas to the same iframe-based renderer as the sidebar, which remains a possible future option if this fix ever proves insufficient): `SmartHtmlEditor.tsx` now watches `document.head` with its own `MutationObserver` for the shared engine's `<style>` rebuild to land (bounded by a 500ms timeout for the common case where no new classes were needed at all), and keeps showing the known-correct iframe fallback (`SmartHtmlSlide`) as an overlay on top of the always-mounted main container until that settles — so the container is never revealed mid-recompile. (Caught and fixed a self-introduced deadlock while building this: an early version gated the container's own *mounting* on `stylesSettled`, which meant the container needed to exist to fire the settle logic but could never mount until it settled — fixed by keeping the container always mounted and using the fallback as a z-indexed overlay instead of an early return.) **Verified visually** via a Playwright script driving the real running dev servers: opened a 10-slide Smart-mode presentation, clicked through 5 different slides (plain text/cards, line chart, bar chart, donut chart, card grid) in the sidebar, and screenshotted the main canvas immediately after each click with zero deliberate wait — the worst-case timing for the race this fix targets. All 5 showed correctly-sized headings matching the sidebar thumbnail's proportions, with no undersized-text artifact in any of them.

   **Follow-up: the same bug recurred and is now fixed deterministically, including on the export path this earlier fix never covered.** Reported again on a title slide using `text-[64px]` (unique in its deck — every other slide used `text-[36px]`), visible both in the editor and in the exported file. The export page's `SmartHtmlPdfSlide` (in `PdfMakerPage.tsx`) had **no settle logic of any kind** — the `MutationObserver`/timeout mitigation above was only ever added to the editor — so any export of a slide with a not-yet-compiled arbitrary size baked the small heading straight into the file; confirmed directly via `getComputedStyle` on `/pdf-maker`, which read back the browser's un-styled default before this fix. Rather than extend the observer-based heuristic to a second component, replaced the *entire mechanism* for font size specifically with a deterministic one: new `lib/smart-slide-arbitrary-styles.ts` exports `applyArbitraryTextStyles(container)`, which — immediately after HTML is injected — parses every element's `text-[...]`/`leading-[...]`/`tracking-[...]` class and writes the equivalent value as an **inline style**. Inline styles win the cascade over any class selector regardless of source order or timing, so the correct size is present on first paint with no race left to lose, in both `SmartHtmlEditor.tsx` (called right after `container.innerHTML = html`) and `SmartHtmlPdfSlide` (called from a `useLayoutEffect`, so it runs synchronously before the browser paints, right after React commits the `dangerouslySetInnerHTML`). When Tailwind's JIT later compiles the identical class, it resolves to the same value, so this only ever fills the gap the JIT hasn't caught up to yet — never overrides a genuinely different one. The original `MutationObserver`/timeout mechanism in `SmartHtmlEditor.tsx` is kept as-is for everything else it was already covering (non-text arbitrary utilities, general Tailwind-recompile timing) — it's just no longer what protects font size specifically. **Verified** via Playwright against the real `/pdf-maker` page for this exact slide: `getComputedStyle(h1).fontSize` read `64px` (matching the source class) consistently across two consecutive checks, with `h1.style.fontSize` confirming the inline style was actually what applied it. 5 new unit tests added in `tests/smart-slide-arbitrary-styles.test.mjs` using hand-stubbed fake DOM elements (no jsdom dependency in this project), covering the three utility mappings, an element with no arbitrary classes, and recursive descendant walking.
4. **Standard-mode ("Generate Standard", plain non-e& Smart) content-overflow bug — fixed.** User-reported "shape/text bleeding off the slide," with 3 screenshots showing content cut off mid-line right at the point where the app's own page background peeked through — no clean card-bottom edge first. Root cause: every Smart slide's root `<section>` carries `h-[720px] overflow-hidden` (enforced at generation time), so content can never *literally* spill past the canvas edge — but plain, unshrunk normal-flow content (a title + several card rows + a footer, none of it `flex-1`/`min-h-0`/absolutely positioned) has no ceiling on its own natural stacked height, and when that natural height exceeds 720px, `overflow-hidden` silently deletes whatever's past the line — which visually reads as content "bleeding"/getting sliced, even though the clip is working exactly as designed. Confirmed by pulling the exact reported slide's real HTML from `app_data/fastapi.db` and running it through `inspect_smart_slide_layout` (the fast, no-render class-analysis checker that already runs on every Smart slide): **zero issues reported**, despite the slide visibly overflowing — because that checker can only compute exact geometry for `position: absolute` elements and a couple of narrow shrink-to-fit patterns; a plain vertical stack that's simply too tall is structurally invisible to it, by its own documented limitation. The one mechanism in this codebase that *can* catch this class of bug — `_check_smart_slide_eand_footer_safe_area`, a real Puppeteer render + pixel check — was wired up only for e&-branded decks, not plain Standard generation, which is what the user was using. Fixed by adding a second, general-purpose real-render check, `_check_smart_slide_canvas_overflow()` in `generate_smart_presentation.py`, that now runs for **every** Smart slide regardless of brand template: it re-renders the slide with only its root section's `overflow-hidden` class removed (see below for why height must stay fixed) against a sentinel `#ff00ff` page background — a color that should never legitimately appear in generated business-deck content — then checks whether any non-sentinel pixel paints below y=720; if so, the slide is rejected with an error fed back into the retry loop, same mechanism as every other Smart-slide validation failure.

   **A real false-positive found and fixed during calibration, worth remembering.** The first implementation stripped *both* `overflow-hidden` and the fixed `h-[720px]` height (switching to `h-auto`) before re-rendering, on the theory that this would let natural content height show cleanly. Tested against 10 real slides from a deck already verified fully clean via live browser screenshots (the font-size-fix testing deck) — and one slide "measured" as overflowing by **390px**, wildly wrong. Root cause: Tailwind's `h-full`/flex-column/grid-stretch sizing throughout a slide's internal layout all depend on that 720px box being a *definite* CSS height — per spec, a percentage height (`h-full` = `height:100%`) against an `auto`-height ancestor doesn't resolve the way authors expect, so switching the root to `h-auto` doesn't just "unclip" the box, it changes how *every descendant's* height computes, producing bogus, wildly inflated measurements especially on slides with nested CSS Grid (`grid-cols-[Npx_1fr]`) + flex-column combinations (common in this app's generated layouts). Fixed by keeping `h-[720px]` intact and removing *only* the `overflow-hidden` class — this preserves every internal layout computation byte-for-byte identical to the real render (the box's own size never changes) while still letting genuinely overflowing content paint past the box and become visible/measurable. **General lesson, same family as the Chart.js-defaults gotcha elsewhere in this file: when building a "re-render with X removed to measure Y" technique, changing more than the one property you actually need to inspect can silently corrupt the very layout you're trying to measure — remove the minimum necessary, and calibrate against real content, not assumptions, before trusting the numbers.**

   Re-calibrated against the same 10 real slides plus the actual reported-broken slide with the corrected (height-preserved) technique: the reported-broken slide measured a real, meaningful overshoot; 4 of the 10 "clean" slides measured exactly zero overshoot (correctly accepted); the other 6 measured real, non-trivial overshoot ranging from ~38px to ~343px. Visually spot-checked 3 of those 6 by rendering the unclipped version to a real PNG (not just trusting the pixel-diff number): all 3 showed genuine, previously-undetected content loss — a chart card's sibling column overflowing by over 300px with two entire cards worth of content pushed past the line in one case, and in two others a footer source-citation/page-number line fully pushed past the boundary and would be completely invisible in the real clipped render. **This means the "already verified clean" 10-slide test deck from earlier in this session was never actually fully clean — 6 of its 10 slides had real, undetected overflow the whole time; the live browser verification earlier only visually spot-checked 5 of the 10 slides (for an unrelated bug, the heading-size fix) and happened to miss the other 5, several of which were among the newly-found-broken ones.** This is a good example of why "the heuristic checker reported zero issues" was never sufficient evidence of correctness for this class of bug, and reinforces why the real-render check needed to exist at all.

   Verified end-to-end: ran the finished `_check_smart_slide_canvas_overflow()` against all 11 real slides (10 from the test deck + the actual user-reported broken one) through the live, unmocked export-render pipeline (not a mock) — correctly rejected the reported-broken slide and the 6 newly-discovered overflowing slides, correctly accepted the 4 genuinely clean ones. Added automated unit test coverage in `tests/unit/test_smart_presentation_generation.py` (HTML class-stripping correctness, pixel-counting math with synthetic images, accept/reject/fail-open behavior with a mocked render) — `_check_smart_slide_eand_footer_safe_area` itself gained equivalent coverage in a later session (see below), closing that gap. **Cost tradeoff, accepted explicitly by the user**: this adds one extra real render (a few seconds) per slide to every Standard-mode Smart generation, matching the cost e& generation already pays for its own footer check — chosen deliberately over a cheaper, no-render heuristic extension, since the heuristic approach is exactly what already missed this bug for 8 slides running.

   **Follow-up: this same check's own bug then broke a real e& generation — root-caused from real backend logs and fixed, including the actual pie/donut content issue it was correctly flagging.** User reported the 14-slide "Chart Stress Test" prompt (see the stress-test prompt below) failing twice via "Generate e& deck," both times stalling around slide 8-10 and eventually erroring "failed to generate the complete Smart presentation after retries; N valid slides were retained." Diagnosed from real pasted backend logs (not speculation): `[smart-generation] attempt=5/8 accepted_slides=6` stayed at 6 through `attempt=6/8` — stuck on slide 7 (the pie chart, "Traffic source breakdown"), being rejected by this exact check's own error text ("The Smart slide's own content is taller than the fixed 720px canvas..."), confirming the render itself succeeded and a *genuine* overflow was detected, not an infra failure.

   Two Explore agents plus direct code verification found two real, independent issues:
   - **The root cause of the actual failure: e&'s content budget is measurably too tight for a pie/donut chart, and nothing told the model that.** e& reserves only `y=48` to `y=630` (582px) for content, vs. plain Standard mode's ~640px. Pulled the existing working Standard-mode pie slide from `app_data/fastapi.db` and measured its real layout: header + `h-[470px]` chart card + gaps + footer totals 711 of 720px available — already a near-zero-margin fit *before* accounting for e&'s smaller budget and its own established pattern of a larger `text-[43px]` headline (vs. Standard's `text-[36px]`, required by e&'s "preserve visual continuity with earlier accepted slides" continuation instruction). That left only ~410-430px for the chart card — 40-60px short of what a legible pie/donut needs, since (unlike bar/line/area) a pie/donut has to stay close to square to remain legible. `CONTENT DESIGN DIRECTION` in `smart_brand_templates.py` already had specific, named overflow-pattern warnings for other known-risky compositions (a full-height side rail, shared card heights) but none for chart-dominant slides. Fixed by adding a new bullet (both to the e&-specific prompt and a lighter version in the general `SMART_OVERFLOW_PREVENTION_PROMPT`) explicitly telling the model a pie/donut chart card needs ~380-420px budgeted *first*, with the header/support content sized to fit around it — not the other way around.
   - **A separate, real bug found while investigating (not what caused this specific failure, but a genuine defense-in-depth fix): infra failures were misclassified as validation failures.** Both `_check_smart_slide_canvas_overflow` and `_check_smart_slide_eand_footer_safe_area` wrapped their render call in `except HTTPException: raise / except Exception: ...return (fail open)` — but `EXPORT_TASK_SERVICE.render_html_to_image` itself raises `HTTPException(500, ...)` on its own 300s timeout or a subprocess crash, so a genuine infra hiccup took the *same* `except HTTPException: raise` path as a real 400 validation failure, aborting the whole attempt and feeding the model an uncorrectable `retry_error` like `"Export task failed. returncode=..."`. Fixed by removing the special-cased re-raise entirely, so **any** exception from the render call (HTTPException included) now hits the fail-open branch; only this function's own post-render validation raise (which happens outside that try, after the render already succeeded) can propagate. Applied identically to both functions, which shared the exact same flaw.
   - Also made the overflow error message itself actionable: `_count_non_sentinel_pixels_below_canvas` already computed a full-resolution thresholded diff image, so extending it to also return `thresholded.getbbox()`'s deepest row (free, no extra render) let the raised message state a concrete "approximately Npx taller than the fixed 720px canvas" instead of generic "too tall" wording the model had no specific target to correct against.

   **Second follow-up: the prompt guidance alone was not enough, so the retry loop itself was made fault-tolerant.** After the above shipped, the same 14-slide e& deck failed again — this time the log showed the pie slide (slide 6) *accepted* with a 440x440 canvas (the guidance demonstrably worked there), but the next chart slide stalled at `attempt=5/8 accepted_slides=6` → `attempt=6/8 accepted_slides=6`, rejected at "approximately 219px taller than the fixed 720px canvas," and generation died again. **The structural problem was never really the prompt: one slide the model cannot get under the threshold consumes every remaining attempt and takes the entire deck down with it.** These render checks are *quality* gates, not correctness gates — a slide that overflows somewhat is vastly better for the user than no deck at all — but nothing in the loop expressed that. Fixed by tracking consecutive failures at the *same* slide position (`stalled_at_index`/`consecutive_stalls`, reset whenever any new slide is accepted, so it only ever triggers on a genuinely stuck position) and, past `SMART_MAX_CONSECUTIVE_SLIDE_FAILURES = 3`, waiving the render-based checks **for that one slide position only** (later slides in the same response are still fully checked) and logging a warning. Hard validity checks — malformed HTML, missing chart initializer, wrong slide type/count — are never waived. Regression test added, and confirmed to genuinely catch the bug: it **fails** when the threshold is disabled and passes with it, plus a companion test proving the waiver is scoped (a response that never yields any usable slide must still fail rather than silently returning a short deck). Also replaced a bare `print(messages)` in the retry loop with `LOGGER.debug` — it dumped the entire prompt (tens of KB, growing with every retry as accepted slide HTML is re-appended) to stdout, which made the backend logs nearly unreadable in exactly the situation where they matter most.

   **Generation-speed profiling (measured, not guessed) — and the one easy win it found.** Instrumented `generate_smart_presentation` to time LLM calls vs. render checks separately across real e& chart generations. Results were consistent across three runs: **LLM calls are ~85-90% of wall time, and the multiplier is retries** (87s/3 attempts on a success; 164s and 206s on 8-attempt failures). Render checks were ~1s each and ~10-16% of total. So micro-optimizing rendering can never fix a slow generation — the lever is not wasting LLM attempts, which is what the stall waiver above addresses. The one genuinely easy render win was real though: **e& slides were rendering the page twice per slide** — `_check_smart_slide_canvas_overflow` and `_check_smart_slide_eand_footer_safe_area` each spawned their own cold Chromium to measure the *same* rendered page. Consolidated into a single `_check_smart_slide_layout(html, *, check_eand_footer)` that renders once and checks two bands of the same image: past y=720 against the sentinel page colour (canvas overflow), and y=630-720 against white (e& footer intrusion, still inside the slide's own box so the comparison is unchanged). Benchmarked directly on a slide that passes both checks (so the old path didn't short-circuit): **2 renders / 1.86-2.08s → 1 render / 0.95-0.96s per e& slide, ~54% faster**, with non-e& unchanged at one render either way. The four now-dead functions and their 10 tests were deleted rather than left as a second implementation that could silently drift.

   **A profiling trap worth remembering: a dead backend makes every render look 10x slower.** The first profiling pass measured a rock-steady ~10.8s per render regardless of slide content — even for a slide containing nothing but `<p>hello</p>` — which looked exactly like a smoking-gun fixed timeout. It was an artifact: the FastAPI server had died mid-session, so the preview page's `<script src="http://127.0.0.1:8000/static/vendor/tailwindcss-browser-*.js">` 404'd. The export bundle's `waitForTailwindCdn` probe (`nnt` in `presentation-export/index.js`) detects Tailwind by the *presence of a script tag matching `/tailwind/i`* — which is still true when the fetch failed — then polls up to **10s** for a probe `<div class="hidden">` to compute `display:none`, which can never happen if the script never loaded. With the server up, Tailwind compiles `.hidden` in ~800ms and renders take ~0.95s. **Always confirm the backend is actually serving `/static/vendor/...` before and after any render benchmark** — a mid-run death silently inflates every number by ~10s and looks like a real finding. Regenerated the identical 14-slide e& prompt against the real running servers via direct API calls (`POST .../presentation/create` then `GET .../presentation/stream/{id}`), capturing the full backend log. This run's log showed the new fix actually firing three times with real, concrete numbers (`approximately 30px`/`222px`/`6px taller than the fixed 720px canvas`) — confirming the message-improvement fix is live — and, critically, **the generation completed successfully this time**: all 15 slides (title + 13 content + closing) persisted, including slide 7 (pie) and slide 8 (donut), within the same 8-attempt budget that previously exhausted itself without ever completing. Slide 7 still needed 2 retries before converging (real content-shape difficulty doesn't vanish entirely from prompt guidance alone), but recovered instead of permanently stalling. Inspected the actual generated pie slide HTML: the model used `text-[36px]` (down from the previous attempt's `43px`) and a `h-[430px]` chart card, matching the new guidance's recommended range almost exactly. Added regression tests for the exception-handling fix specifically (an `HTTPException(500)` from the render call must now fail open, for both checks — the previous test suite only covered a generic `RuntimeError`, which is exactly the gap that let this bug ship unnoticed) and for the new prompt guidance being present in both the general and e&-specific prompt output.
5. **Open: Smart-mode charts intermittently render blank in the live editor while a deck is still streaming.** Reported with a screenshot: during an e& generation, slides 3-4 appeared in both the sidebar thumbnails and the main canvas with their chart cards completely empty (border and card title present, no chart), then the charts "randomly showed up again" once slide 6 finished generating. Self-healing, so cosmetic rather than blocking, but real. Partially investigated and two plausible causes **ruled out**: (a) `loadChartBrowserRuntime()` in `lib/chart-browser.ts` is correctly singleton-cached on `window.__PRESENTON_CHART_BROWSER_RUNTIME__` and only resolves after `window.Chart`/`window.ChartDataLabels` are actually assigned, so the injected chart scripts cannot be seeing an undefined `Chart`; (b) effect ordering in `SmartHtmlEditor.tsx` is fine — the `container.innerHTML = html` effect (~:331) is declared before `useSmartChartInjection` (~:644), so the container is populated before charts are injected. Remaining suspects worth checking next: the injected script's root lookup in `useSmartChartInjection.ts` (`document.querySelectorAll('[data-smart-slide-instance]')` + `.find(...)`, which **silently `return`s if no match** and would also bind to the wrong element if two mounted components ever share an `instanceId` — e.g. the same slide rendered in both the sidebar thumbnail and the main canvas), and whether the sidebar's iframe-based `SmartHtmlSlide` path has its own separate race for slides that arrive mid-stream.
6. **Open (next session): make Smart generation faster by not discarding a whole LLM response on one bad slide — but MEASURE FIRST, this may not be worth building.**

   Profiling (documented under item 4) established the cost model: **LLM calls are ~85-90% of wall time and the multiplier is retries** — each failed attempt throws away a full ~20-30s LLM call. Render checks are ~1s each and were already halved for e& by consolidating the two per-slide renders into one, so there is nothing meaningful left to win there.

   The remaining structural waste: when a slide fails validation, `handle_chunk` raises, which aborts consumption of the **entire rest of that response**. The model may have already generated several perfectly good slides after the failing one; all of them are discarded and regenerated from scratch next attempt.

   **Do this first, before writing any salvage code:** instrument the retry loop to log *which slide index* each attempt fails at, and run the 14-slide chart stress-test prompt (below) a few times. The payoff scales entirely with how late failures land — a failure at slide 7 of 14 could salvage ~6 slides, but in the profiled 5-slide runs failures hit at slides 1-3, where there was essentially nothing downstream to save. If failures cluster early, **the salvage work is not worth it** and the better lever is reducing the failure *rate* (prompt guidance, or tuning `SMART_MAX_CONSECUTIVE_SLIDE_FAILURES`, currently 3).

   If the data does justify it, the hard constraint to design around: **slides are positionally ordered and the continuation prompt treats `accepted_slides` as an immutable prefix**, so slides 8-13 cannot simply be accepted when slide 7 failed. Salvaging requires either regenerating only the failed slide while holding the later ones aside, or accepting the failed slide via the existing waiver path so the prefix stays contiguous. Relevant code: the `for _attempt in range(SMART_GENERATION_MAX_ATTEMPTS)` loop, `handle_chunk`, and the `accepted_slides.extend(attempt_slides)` failure branch in `utils/llm_calls/generate_smart_presentation.py`.
7. **`/generation` page button-label cleanup.** "Generate Standard" doesn't generate Standard/TemplateV2 — it generates Smart mode without e& branding. Real TemplateV2 generation only exists behind the "Outline" button. See the architecture note above; worth relabeling so the UI doesn't contradict itself.
8. **Project health, lower priority**: fix the broken Electron `COPY` lines in both Dockerfiles; add the root `npm install` step to README's setup instructions; delete or document the dead `/generate/async` presentation endpoint; add test coverage — several past-session additions still have none (SSE route handlers, the sidebar-selection-sync fix, the regenerate-slide flow, `presentation_layout_resolver.py`, `getStreamProgressLabel`, `templates/pptx_color_extraction.py`), only manual/live verification. (`smart_slide_layout.py`'s overflow heuristics and e& slide-count semantics now have real automated coverage — `tests/unit/test_smart_slide_layout.py`, `tests/unit/test_smart_presentation_generation.py`; the native PPTX chart export work has unit coverage too — `tests/unit/test_pptx_native_chart_service.py`, `tests/unit/test_chart_capture_store.py`, `tests/unit/test_chart_capture_endpoint.py`, `tests/chart-export-capture.test.mjs`.)

### GenAI test prompt (for re-testing chart export)

10-slide GenAI deck prompt used to test native chart export, covering bar/line/donut/radar (should convert to native charts) and one scatter chart (should stay a flattened image — that's correct, not a bug):

> Create a 10-slide presentation on Generative AI (GenAI) — its growth, business adoption, risks, and future outlook. Use real charts (not decorative images) wherever data is mentioned. Structure:
>
> 1. Title slide: "Generative AI: Transforming Business in the AI Era"
> 2. What is GenAI: Define generative AI, contrast with traditional/predictive AI, briefly mention key model types (LLMs, diffusion models, multimodal models).
> 3. Market growth — include a line chart: Global GenAI market size in $B for 2023: 45, 2024: 67, 2025: 98, 2026: 142, 2027: 205, 2028: 295.
> 4. Industry adoption rates — include a bar chart: % of companies using GenAI by industry: Tech 78%, Financial Services 61%, Healthcare 48%, Retail 44%, Manufacturing 39%, Education 33%.
> 5. Leading model providers by market share — include a donut chart: OpenAI 38%, Google 22%, Anthropic 15%, Meta 12%, Others 13%.
> 6. Top business use cases: Content creation, customer support automation, code generation, data analysis, personalized marketing. Brief description of each, no chart needed.
> 7. VC investment trends — include a bar chart: Global GenAI VC funding in $B: 2021: 3, 2022: 8, 2023: 21, 2024: 33.
> 8. Key risk factors — include a radar chart rating five risk categories on a 1-10 severity scale: Data Privacy 7, Bias & Fairness 8, Misinformation 9, IP/Copyright 7, Job Displacement 6.
> 9. AI maturity vs. revenue growth correlation — include a scatter chart plotting AI maturity score (1-10) against YoY revenue growth (%) for 8 sample companies, showing a general positive trend.
> 10. Future outlook & call to action: Summarize where GenAI is headed in the next 3 years and a closing recommendation for the audience.

Note: on the live `/generation` page's "Generate Standard" button, this is Smart mode (see backlog item 7) and the e&-footer-safe-area check does not apply (only relevant via "Generate e& deck") — though the general canvas-overflow check (backlog item 4) now applies to it.

### Full chart-type stress-test prompt (for re-testing chart export against every supported kind)

The GenAI prompt above only exercises bar/line/donut/radar/scatter. This 14-slide prompt is a more thorough successor built specifically to cover the full native-chart-export support matrix in one generation — all 9 kinds `pptx_native_chart_service.py` converts to native, editable PowerPoint charts (bar, horizontal bar, stacked bar, horizontal stacked bar, line, area, pie, donut, radar), plus the 3 kinds it deliberately excludes (scatter, polar area, bubble — no faithful native-PPTX equivalent, so they should stay flattened images; that's correct, not a bug):

> Create a 14-slide presentation titled "Chart Stress Test" that tests every chart type. Use real charts wherever data is mentioned, one distinct chart type per slide. Structure:
>
> 1. Title slide: "Chart Stress Test: Every Chart Type in One Deck"
> 2. Vertical bar chart: Regional Q4 revenue in $M — North 42, South 35, East 58, West 61
> 3. Horizontal bar chart: Feature request votes — Dark mode 120, Offline mode 95, Bulk export 80, API access 65, Custom themes 40
> 4. Stacked bar chart: Support tickets by channel per month — Jan (Email 30, Chat 45, Phone 15), Feb (Email 35, Chat 50, Phone 20), Mar (Email 28, Chat 60, Phone 18)
> 5. Horizontal stacked bar chart: Weekly hours by team and activity — Engineering (Dev 25, Meetings 8, Support 7), Design (Dev 20, Meetings 10, Support 5), Product (Dev 15, Meetings 15, Support 10)
> 6. Line chart: Daily active users over 7 days — Mon 1200, Tue 1250, Wed 1180, Thu 1300, Fri 1400, Sat 1100, Sun 1050
> 7. Area chart: Cumulative signups over 6 months — Jan 500, Feb 1200, Mar 2100, Apr 3400, May 5000, Jun 7200
> 8. Pie chart: Traffic source breakdown — Organic 45%, Paid 25%, Referral 15%, Social 10%, Direct 5%
> 9. Donut chart: Device type breakdown — Mobile 55%, Desktop 35%, Tablet 10%
> 10. Radar chart: Product readiness scores (1–10 scale) — Performance 8, Security 9, Usability 6, Documentation 5, Scalability 7
> 11. Scatter chart: Marketing spend ($K) vs. conversions for 8 campaigns, showing a rough positive trend
> 12. Polar area chart: Skill distribution across 5 categories — Frontend 8, Backend 9, DevOps 6, Design 5, Data 7
> 13. Bubble chart: Team size vs. project count, bubble size = budget, for 6 sample teams
> 14. Closing slide: "Summary" — brief wrap-up, no chart needed

Expected after export: slides 2-10 (9 kinds) convert to native charts; slides 11-13 (scatter/polar area/bubble) stay pictures.
